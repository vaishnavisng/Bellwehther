# Bellwether — runtime dependencies (deployment-safe).
# Floors chosen so pip resolves fast and only picks versions with modern wheels.

# Core
pandas>=2.2
numpy>=1.26
pyyaml>=6.0

# Storage
duckdb>=1.0

# NLP
scikit-learn>=1.4

# Statistics / predictive analytics
scipy>=1.11
statsmodels>=0.14

# Visualization / dashboard
plotly>=5.20
streamlit>=1.36

# Ingestion — Google Play (pure Python, no heavy transitive deps)
google-play-scraper>=1.2

# NOTE: Apple App Store ingestion uses `app-store-scraper`, which is unmaintained
# and drags in an ancient requests/urllib3 stack (breaks on modern Python and
# stalls dependency resolution). It is intentionally NOT installed here.
# The adapter lazy-imports it, so everything else works without it. To enable
# App Store ingestion locally:  pip install app-store-scraper
#
# Dev/test extras (not needed to run the app):  pip install pytest
