# Deployment compatibility: force modern urllib3 so the old vendored-six import
# (urllib3.packages.six.moves) can't break the build on newer Python.
urllib3>=2
# Some deps (scrapers) import pkg_resources at import time; not bundled by
# default on newer Python/pip.
setuptools

# Core
pandas
numpy
pyyaml

# Storage
duckdb

# NLP (later layers)
scikit-learn

# Statistics / predictive analytics (later layers)
scipy
statsmodels

# Visualization / dashboard
plotly
streamlit

# Ingestion adapters (Google Play + Apple App Store)
google-play-scraper
app-store-scraper

# Tests
pytest
